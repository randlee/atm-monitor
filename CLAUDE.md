# atm-monitor agent guidance

Read [AGENTS.md](AGENTS.md) for repository conventions and skill routing.
For ATM template types, tags, workflow query metadata, or pre-push remediation,
use `/atm-template-maintainence` and follow the authoritative skill at
`skills/atm-template-maintainence/SKILL.md`. The `.claude/skills/` entrypoint
loads that skill; it does not maintain a separate standard.
